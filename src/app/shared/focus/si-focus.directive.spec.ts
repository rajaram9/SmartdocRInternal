import { SiFocusDirective } from './si-focus.directive';

describe('SiFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new SiFocusDirective();
    expect(directive).toBeTruthy();
  });
});
